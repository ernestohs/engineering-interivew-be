import { NextFunction, Request, Response } from 'express';
export function reportTaskByStatus(
  req: Request,
  res: Response,
  next: NextFunction,
) {}
export function reportSummary(
  req: Request,
  res: Response,
  next: NextFunction,
) {}
export function reportTaskByDate(
  req: Request,
  res: Response,
  next: NextFunction,
) {}
